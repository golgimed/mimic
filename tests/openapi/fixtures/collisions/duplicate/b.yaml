openapi: 3.0.3
info:
  title: Dup Spec
  version: 1.0.0
paths:
  /b:
    get:
      responses:
        "200":
          content:
            application/json:
              example:
                from: b
